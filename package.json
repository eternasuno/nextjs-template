{
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.24.0",
    "nodemon": "^3.1.7",
    "purescript": "^0.15.15",
    "purs-tidy": "^0.11.0",
    "spago": "^0.93.41"
  },
  "scripts": {
    "build": "spago bundle --platform node --outfile index.mjs",
    "dev": "nodemon -w src -e purs,js --exec spago run",
    "start": "node index.mjs",
    "test": "spago test"
  }
}